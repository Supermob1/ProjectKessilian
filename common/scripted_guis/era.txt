scripted_gui = {

	era_main_window = {
		context_type = player_context
		parent_window_token = top_bar
		window_name = "era_main_window"
		dirty = era_refresh
		visible = {
		}
		properties = {
			credits_window = {
				frame = creditsamount_frames
			}
		}
	}
}

