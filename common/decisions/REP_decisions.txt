REP_the_Grand_Army = {
	REP_Prepare_the_army = {
        cost = modifier@Kamino_delivery_price
		fire_only_once = no
		days_remove = 25
		available = {
			has_army_size = {
				size < 190
				type = infantry
			}
		}
        icon = generic_industry
		ai_will_do = {
			factor = 100
		}
		modifier = { power_balance_daily = -0.006 }
		remove_effect = {
			set_temp_variable = { max_units = modifier@Kamino_delivery_capacity }
			set_temp_variable = { exp_val = modifier@Kamino_delivery_experience }
			set_temp_variable = { eqp_val = modifier@Kamino_delivery_equipment }
			custom_effect_tooltip = REP_prepare_army_tooltip
			for_loop_effect = {
				start = 1
				end = max_units
				compare = less_than_or_equals
				add = 1
				value = iteration_count 
				
				random_owned_controlled_state = {
					limit = { is_core_of = REP }
					
					meta_effect = {
						text = {
							create_unit = {
								division = "division_template = \"Clone Legion\" start_manpower_factor = 1 start_experience_factor = [EXP_TOKEN] start_equipment_factor = [EQP_TOKEN]"
								owner = REP
							}
						}
						EXP_TOKEN = "[?exp_val|.2]" 
						EQP_TOKEN = "[?eqp_val|.2]" 
					}
				}
			}
		}
	}
	REP_rollout_phase_II = {
        cost = modifier@Kamino_delivery_price
		fire_only_once = no
		available = {
		}
        icon = generic_industry
		ai_will_do = {
			factor = 100
		}
		complete_effect = {
			set_cosmetic_tag = REP_PHASE_II
		}
	}
	# REP_divert_funds_to_kamino #increase capacity
	# REP_diversify_clone_training #decrease capacity but allow specialized units
	# REP_rollout_phase_two_units #change the template to use both the new phase 2 clone troopers
	# REP_protect_kamino #build forts on kamino main cities
	# REP_i_dont_know #increase capacity
	# REP_idk2 #permanently increase capacity by one in exchange of decision cost increase
	# REP_idk3 #permanently increase overall exp in exchange of higher power balance costa
}


