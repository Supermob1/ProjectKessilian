generic_debug = {

	# Manually start a duel between random assigned commanders on each side.
	# Selection priority: Force-sensitives > skilled_duelists > any assigned commander.
	HOD_debug_duel_random = {
		cost = 0
		fire_only_once = no
		visible = {
			has_war = yes
		}
		available = {
			any_army_leader = { is_assigned = yes }
			any_enemy_country = {
				any_army_leader = { 
					is_assigned = yes 
				}
			}
		}
		icon = generic_spy
		complete_effect = {
			country_event = { id = hod_duel.1 days = 1 }
		}
	}
}
